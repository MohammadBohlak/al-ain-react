import styled from "styled-components";

const StyledAbout = styled.section`
  padding-top: var(--nav-height);
  picture {
    display: block;
    margin: auto;
    width: 25%;
  }
`;

export { StyledAbout };
